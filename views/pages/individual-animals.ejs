<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= title %></title>
</head>
<body>
    <h1><%= animal.name %></h1>
    <img src="<%= animal.image %>" alt="<%= animal.name %>" width="200">
    <p><strong>Scientific Name:</strong> <%= animal.scientificName %></p>
    <p><strong>Lifespan:</strong> <%= animal.lifespan %></p>
    <p><strong>Food:</strong> <%= animal.food %></p>
    <p><strong>Found In:</strong> <%= animal.found %></p>
    <p><strong>Length:</strong> <%= animal.length %></p>
    <p><strong>Weight:</strong> <%= animal.weight %></p>
    <p><strong>Description:</strong> <%= animal.description %></p>
    <a href="<%= animal.groupPage %>">Back to All Reptiles</a>
</body>
</html>
